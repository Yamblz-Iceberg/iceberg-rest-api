doctype html
html(lang='en')
  head
    title Callback
    body= response
    script(type='text/javascript').
      var app = 'iceberg';
      var str = "";
      for (var key in !{response}) {
      if (str != "") {
      str += "&";
      }
      str += key + "=" + encodeURIComponent(!{response}[key]);
      }
      window.location = app + '://?data=' + str;